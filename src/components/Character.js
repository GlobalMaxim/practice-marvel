import React from 'react'

const Character = (props) => {
    
        return(
            <div>
                {props.name}
                {props.id}
            </div>
        )
    
}
export default Character